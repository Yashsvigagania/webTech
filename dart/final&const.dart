void main(){
  final String country="India";
  const double pi=3.14;
  final time=DateTime.now();
  final students=DateTime.now().hour;
  // const time = DateTime.now(); ❌ error
  //"Each section will always have 60 students."
  const maxStudents = 60;
  print(maxStudents);
  print(country);
  print(pi);
  print(time);
  print(students);
}